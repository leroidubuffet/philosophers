#include "philo.h"

// Add your utility functions related to number manipulation here
// For example:

// Function to check if a number is even
int is_even(int num) {
    return num % 2 == 0;
}

// Function to check if a number is odd
int is_odd(int num) {
    return num % 2 != 0;
}

// Function to calculate the square of a number
int square(int num) {
    return num * num;
}

// Add more utility functions as needed